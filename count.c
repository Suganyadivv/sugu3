#include <stdio.h>

int main()
{ 
  //  char ch[10];
  // scanf("%s",ch);
    int count,i;
    scanf("%d",&count);
    for(i=0;i<count;i++)
    {
        printf("Hello");
        printf("\n");
    }
    return 0;
}
